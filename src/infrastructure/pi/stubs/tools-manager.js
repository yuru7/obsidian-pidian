/**
 * Pi downloads fd/ripgrep ZIP archives and extracts them. Pidian does not use
 * those binaries; note search goes through the Obsidian vault API.
 */
export function getToolPath() {
  return null;
}

export async function ensureTool() {
  return null;
}
